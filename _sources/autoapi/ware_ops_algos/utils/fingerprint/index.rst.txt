ware_ops_algos.utils.fingerprint
================================

.. py:module:: ware_ops_algos.utils.fingerprint


Functions
---------

.. autoapisummary::

   ware_ops_algos.utils.fingerprint.fingerprint
   ware_ops_algos.utils.fingerprint.import_all
   ware_ops_algos.utils.fingerprint.inheritors
   ware_ops_algos.utils.fingerprint.build_map


Module Contents
---------------

.. py:function:: fingerprint(cls: type, *, length: int = 12) -> str

   Hash of a class: its source plus its in-package bases, so a change to an
   inherited method counts too.
   Bases outside the algorithm's own top-level
   package (object, abc.ABC, third-party) are skipped.

   `ast.unparse` regenerates canonical source from the AST, so comments and
   formatting drop out while the hash stays stable across Python versions.


.. py:function:: import_all(package) -> None

   Import every submodule so __subclasses__() sees all algorithms. Skip this
   if your package __init__ already imports them.


.. py:function:: inheritors(base: type) -> set[type]

.. py:function:: build_map(base: type) -> dict[str, str]

   {qualified name -> hash} for every subclass of `base`.


