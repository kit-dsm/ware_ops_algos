algo_name: CombinedBatchingRoutingAssigning
problem_type: batching_routing
description: >
  Exact MIP for the joint order batching and picker routing
  problem. Assigns orders to batches and solves routing per
  batch simultaneously using Gurobi.

requirements:
  layout:
    type:
      - any
    features:
      - start_node
      - end_node
      - distance_matrix
      - predecessor_matrix
  resources:
    type:
      - any
    features:
      - capacities
      - dimensions_type
      - n_boxes
    constraints:
      dimensions_type:
        equals: [items]
      n_boxes:
        equals: 1
  orders:
    type:
      - any
  storage:
    type:
      - any

objective: Distance

implementation:
  class_name: CombinedBatchingRoutingAssigning
  solver: Gurobi
  type: exact
