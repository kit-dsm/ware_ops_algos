model_name: ExactSolving
problem_type: routing
description: >
  Exact TSP-based routing algorithm using Gurobi for order picking,
  minimizing total travel distance.

requirements:
  layout:
    type:
      - any
    features:
      - distance_matrix
  resources:
    type:
      - any
    features:
  orders:
    type:
      - standard
    features:
      -

  storage:
    type:
      - any
    features:
      -

objective: Distance

implementation:
  class_name: ExactTSPRoutingDistance
  solver: Gurobi
  type: exact
