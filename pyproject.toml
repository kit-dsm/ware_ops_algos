[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ware_ops_algos"
version = "0.1.0"
description = "Warehouse optimization algorithms, domain models, and algorithm cards for CASOP."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
authors = [{ name = "Janik Bischoff" }]
dependencies = [
    "gurobipy",
    "matplotlib",
    "networkx",
    "numpy",
    "pandas",
    "scipy",
    "seaborn",
    "PyYAML",
    "alns",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-benchmark",
]

docs = [
    "sphinx",
    "sphinx-copybutton",
    "furo",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"ware_ops_algos" = [
    "algorithms/algorithm_cards/*.yaml",
    "algorithms/algorithm_cards/*.yml",
]
